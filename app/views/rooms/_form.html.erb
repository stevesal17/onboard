<%= simple_form_for @room do |form| %>

	<%= form.input :name %>
	<%= form.input :address %>
	<%= form.input :number_of_beds %>
	<%= form.input :price_in_pence %>
	<%= form.input :is_available %>

	<%= form.button :submit %>
	


<% end %>
